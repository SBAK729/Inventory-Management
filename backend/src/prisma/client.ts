// Every other file in src/ imports Prisma types and the PrismaClient class
// from here, instead of reaching into ../../generated/prisma/client directly.
// If the generator's output path ever changes, this is the only line to update.
export * from '../../generated/prisma/client';
